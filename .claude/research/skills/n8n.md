---
title: n8n (Workflow Automation)
type: skill
verdict: weak
---
## Evidence
- [[bluparrot]] — Vapi "Shambhu" agent spec: self-hosted n8n (Docker) designed as the workflow engine (poll sheet → fire call → parse webhook → write back → WhatsApp alert). **Spec-only on disk — no n8n workflow exports exist** (Vapi honesty note).

## Resume verdict
Do not list as a skill. At most, "designed an n8n-based call-orchestration workflow (POC spec)" inside the Vapi project description. Tejas's own ruling: moderate at best, and with zero artifacts on disk this lands at weak.

## Interview readiness
Can describe the designed data path and webhook handling conceptually. If asked "did you build the n8n flows?", the honest answer is: designed, not evidenced as built.
