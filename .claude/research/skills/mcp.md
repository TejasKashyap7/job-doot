---
title: MCP (Model Context Protocol)
type: skill
verdict: moderate
---
## Evidence
- [[bluparrot]] — Smart Query Assistant: Wikipedia retrieval via `mcp` + `langchain-mcp-adapters` inside a LangGraph StateGraph; Tejas also built a custom Wikipedia MCP server for it [verified-in-code].

## Resume verdict
Yes for resume (MCP is a hot keyword and the usage is real) — phrase as "built a custom Wikipedia MCP server and wired it into a LangGraph multi-source RAG pipeline". Single-project evidence keeps it moderate, not strong.

## Interview readiness
Can explain MCP server/client roles, tool exposure, and adapter integration with LangChain. Don't claim broad MCP ecosystem experience (multiple servers, auth, resources) — one server, one consumer.
