---
title: Vite
type: skill
verdict: moderate
---
## Evidence
- [[lens]] — Vite 5.3.4 + @vitejs/plugin-react as the build/dev toolchain; `tsc && vite build` production build exists on disk (`ui/dist/`) [verified-in-code: `ui/package.json`, `ui/vite.config.ts`].

## Resume verdict
Tooling, not a skill line. Mention only inside a frontend-stack phrase ("React/TypeScript/Vite/Tailwind") if a JD asks for it.

## Interview readiness
Standard usage: dev server on 5173, production builds. No custom plugin/config work evidenced — keep claims minimal.
